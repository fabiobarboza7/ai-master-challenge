import type { NextConfig } from "next"

// Exportação estática: o protótipo roda inteiro no navegador (modelo em public/data/model.json).
const nextConfig: NextConfig = { output: "export" }

export default nextConfig
